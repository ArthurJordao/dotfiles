#!/bin/bash
# Install this host's declared packages: packages/<family>/<group>.txt, selected by
# role. Run it explicitly via `just packages` (or `just upgrade`, which does a full
# system upgrade first). The same body runs once at bootstrap from
# run_once_install-packages.sh.tmpl; after that this is the only path, since
# the hook deliberately does not re-fire when a package list changes.
{{ template "install-packages.sh" . }}
