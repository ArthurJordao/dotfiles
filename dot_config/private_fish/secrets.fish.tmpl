{{- /* `quote` is right here and wrong in the podman env files. */ -}}
{{- range $name, $field := (onepasswordItemFields .secrets.items.shell .secrets.vault) -}}
set -x {{ $name }} {{ $field.value | quote }}
{{ end -}}
