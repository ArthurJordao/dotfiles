- defaults:
    link:
      create: true
      relink: true
- clean: ['~', '.config']
- link: 
    ~/.zshrc:
    ~/.config/lvim: lvim
    ~/.config/kitty: kitty
    ~/.gitignore:
    ~/.gitconfig:
- shell: []
