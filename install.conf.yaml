- defaults:
    link:
      create: true
      relink: true
- clean: ["~", ".config"]
- link:
    ~/.config/zsh: zsh
    ~/.gitignore:
    ~/.gitconfig:
    ~/Pictures/wallpaper.png: wallpaper.png
    ~/.profile:
    ~/.zprofile:
    ~/Brewfile:
    ~/.config/nvim/lua/user: astrovim-config
    ~/.config/alacritty: alacritty
    ~/.config/tmux: tmux
- shell:
    - osascript -e "tell application \"System Events\" to tell every desktop to set picture to \"~/Pictures/wallpaper.png\" as POSIX file"
    - brew bundle --file ~/Brewfile
    - if [ $0 != "-zsh" ]; then chsh -s /bin/zsh; fi
