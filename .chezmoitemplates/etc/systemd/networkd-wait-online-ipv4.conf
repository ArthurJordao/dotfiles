# GENERATED from .chezmoidata/etc.yaml.
# Overrides Armbian's 20s cap, which returns before DHCP finishes on a cold boot.
# Must sort after armbian-any-interface.conf. 60s and no more:
# podman-user-wait-network-online caps its own wait at 90s.
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --ipv4 --timeout=60
